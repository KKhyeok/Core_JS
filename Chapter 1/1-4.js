var obj1 = {
    a: 1,
    b: 'bbb'
};

// a, b는 identifier or property(key)이고, 1과 'bbb' 는 data or value라고 한다.
// obj1 관점에서는 {}내용이 reference type의 data이다.
// obj1, a, b 는 identifier area에 있고, {}, 1, 'bbb' 는 data area에 존재한다.