var a = 'abc'
a = a+'def'

var b = 5;
var c = 5; // data값인 5를 새로 만들지 않고 data 영역에 있는 5 를 사용
b = 7;


// 기본형 데이터는 불변값이기때문에 다른 값으로 변경할 수 없다.
// identifier가 variable이라면 바꿀 수 있다.
// 변수(variable)은 바꿀 수 있고, 상수(constant)는 바꿀 수 없다.

